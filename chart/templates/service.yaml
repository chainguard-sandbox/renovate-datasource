apiVersion: v1
kind: Service
metadata:
  name: {{ include "renovate-datasource.fullname" . }}
  labels:
    {{- include "renovate-datasource.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "renovate-datasource.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
